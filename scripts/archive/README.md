# Archived one-off scripts

Do **not** run these in production without review. They are not in package.json.

- **mark-hired.ts** – One-time: mark specific candidates as HIRED.
- **check-attendance-sync.ts** – One-time: check attendance sync state.
- **remove-old-training-task.ts** – One-time: remove an old training task.
- **load-onboarding-pdfs.ts** – One-time: load onboarding PDFs.
- **add-onboarding-folder-task.ts** – One-time: add onboarding folder task.

Active scripts (in package.json): `clear-rbts`, `backfill-onboarding-tasks`, `fix-hired-rbts`, `add-admin`.
