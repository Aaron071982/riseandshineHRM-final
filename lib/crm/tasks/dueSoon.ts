/**
 * @deprecated Prefer sendCrmTaskDigests (bi-nightly Resend digest).
 * Kept for manual/ad-hoc use; due-soon is now a digest section.
 */
export { sendCrmTaskDigests as sendDueSoonTaskReminders } from '@/lib/crm/tasks/taskDigest'
